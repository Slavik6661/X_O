/* export type Game = {
  roomId: string;
  Gameslobby: string[];
  gameField: string[][];
  fieldSize: string | null;
  step: "X" | "0";
};

export const createGame = (): Game => ({
  roomId: Date.now().toString().slice(8),
  Gameslobby: [],
  gameField: [],
  fieldSize: null,
  step: "X",
}); */
